# 2026-06-15

## 昆仑谣_画面一_交互.html
- 字幕字号放大：`.sub-line` font-size 从 `clamp(10px,2.2vw,13px)` → `clamp(14px,3.5vw,24px)`
- 王母娘娘(s6)字幕改为右侧竖排：新增 `#sub.sub-right` 样式（writing-mode:vertical-rl, right:5%），JS中s6触发时添加class
- **宽屏适配**：原图全是1024×1024正方形，电脑16:9屏幕下cover裁切严重。用PIL批量处理为1920×1080宽屏版（原图高度填满居中，两侧用模糊暗化原图填充），存入 `kunlun-images/widescreen/`，更新HTML和JS预加载路径

## flower-cam.html（2026-05-14-task-1目录）
- 修复 `const HALF_W` → `let HALF_W`，resize不再报错
- 视频opacity从0.06提到0.12，brightness从0.3提到0.5，能看到摄像头画面
- 肤色检测从1条严格规则放宽为3条（普通/强光/偏黄），阈值从80降到60
- 新增右上角调试面板：显示摄像头状态、视频帧尺寸、肤色像素数、手部坐标
- startCamera增加详细错误提示（权限拒绝/设备未找到/被占用/file协议等）

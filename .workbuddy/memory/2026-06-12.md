# 2026-06-12 工作日志

## 昆仑谣 - 青鸟交互延长 & v7升级

### 时间线延长
- s1: dur 9000 → 22000（22秒）
- s2: dur 7500 → 18000（18秒）
- 总计从16.5秒延长至40秒，参考用户提供的15.3秒录屏交互时长

### SCENE_HINTS提示更新
- s1: 出拳·青鸟振翅 → 伸手引鸟（伸手向青鸟，它会飞向你）
- s2: 挥手切幕 → 伸手控鸟（手近鸟大，手远鸟小，移动手引导鸟飞行）

### birdKiteTick v7 重写
- blend渐入：0.015→0.025（~0.67秒更快响应）
- blend渐出：0.008→0.006（~2.8秒更慢放手）
- posLerp：0.012+0.08*blend → 0.015+0.10*blend（更灵敏跟随）
- EMA kSmooth：0.1→0.12（更快手部坐标响应）
- 自治漂浮homeX/Y：叠加3层正弦波更丰富呼吸感
- 新增灵丝连线数据 _spiritThread（blend>0.3时激活）
- hasHand阈值：0.08→0.06（手刚入画即响应）

### 灵丝连线绘制（bird.draw新增）
- 手-鸟间发光贝塞尔曲线（双层：外线1.8px + 内芯0.8px）
- 沿线6个光点采样，alpha随正弦脉动
- screen混合模式，blend 0.3→1 映射 alpha 0→0.55

### 技术备注
- 文件中Unicode以\\uXXXX转义字面量存储（非实际Unicode字符），直接用Edit工具匹配中文会失败
- 需用Python脚本按行处理或用转义序列匹配

## 昆仑谣 - 字幕逐字打字机效果

### 改动
- CSS: 去掉inter整体interIn动画，改为`.inter-char`逐字动画（charIn: translateY(6px)→none + opacity）
- JS: inter渲染从一次性innerHTML改为逐字创建`<span class="inter-char">`
- `<br>`标签转为`<span class="inter-br">`保持换行
- charDelay=60ms/字, baseDelay=600ms（等容器淡入完成）
- 所有定时器push到_pendingShows，场景切换时自动清理

### inter时长调整
- 前四段短文(38-62字符): 保持dur=5500ms，余量1-2.6秒
- 「文字不死」(150字符): dur=5500→11000ms
- 「该回去了」(180字符): dur=5500→12000ms
- 「始」(6字符): 保持dur=4500ms

## 昆仑谣 - 叙事小字(sub)逐字打字机效果

### 改动
- CSS: 去掉`.sub-line`整体`subRise`动画，避免与逐字动画冲突
- CSS新增`.sub-char`逐字动画（subCharIn: translateY(4px)→none + opacity, 0.35s）
- JS: sub渲染从一次性`textContent`改为逐字创建`<span class="sub-char">`
- charDelay=45ms/字（比inter更快，符合小字阅读节奏）
- 所有定时器push到`_pendingShows`，场景切换时自动清理

## 昆仑谣 - 青鸟缩放修复

### 问题
1. 离屏Canvas(200×160)固定大小，鸟放大时被裁切（头/冠羽/尾巴截断）
2. 缩放逻辑存在但因裁切看不出效果

### 改动
- draw()离屏Canvas改为动态尺寸：`cw=max(250,ceil(180*s+40))`, `ch=max(180,ceil(90*s+40))`
- Canvas仅放大不缩小（`<cw`时resize），避免频繁重建
- translate/drawImage改用动态中心 `_ocacheCX/CY`
- birdKiteTick v8缩放：kiteScale=`0.5+prox*3.2`（原0.6+prox*2.4），lerp=`0.04+0.12*blend`
- 透视Y偏移加强：kiteBaseY=`0.6-prox*0.4`，系数0.05
- 翅膀速度：`0.04+prox*0.16`

## 昆仑谣 - 青鸟飞走问题修复

### 问题
s1/s2鸟场景中，一检测到手就"飞走"——实际是挥手手势触发了切场景

### 改动
1. 挥手(`open`)在鸟场景中不再切场景，改为释放星光粒子
2. 手势判定阈值在鸟场景中提高：dirX阈值从1.2→3.5，避免正常伸手互动被误判为挥手
3. 出拳(`fist`)在鸟场景中已有粒子特效，不切场景，保持不变

## 昆仑谣 - 青鸟远近交互v9修复

### 问题
1. 鸟只能上下移动，不能远近移动——Y位置跟随_handCenterNY(手画面Y坐标)而非proximity(深度)
2. 大小变化不明显——proxYOffset系数仅0.05，缩放lerp太慢
3. 场景时间过长——s1=22s, s2=18s

### 改动
- birdKiteTick v9：Y不再跟_handCenterNY，改为跟proximity(深度)
  - depthYShift = prox * H * 0.32（手近→鸟下移32%屏幕高，飞近镜头感）
  - 删除_smoothKiteY和kiteY逻辑
- 缩放lerp加速：0.04+0.12*blend → 0.08+0.22*blend
- 位置lerp加速：0.015+0.10*blend → 0.03+0.14*blend
- EMA kSmooth：0.12 → 0.18
- 场景时长：s1=22s→12s, s2=18s→10s

## 昆仑谣 - 青鸟交互v10极速加速

### 问题
用户反馈青鸟手势交互不够灵敏，响应迟钝

### 改动 (birdKiteTick v10)
- hasHand阈值：0.06 → 0.03（更低触发）
- blend渐入：0.025 → 0.06（~0.28秒激活）
- blend渐出：0.006 → 0.015（~1.1秒释放）
- EMA kSmooth：0.18 → 0.30（手部坐标极速跟随）
- posLerp：0.03+0.14*blend → 0.06+0.22*blend（位置跟踪翻倍）
- scale lerp：0.08+0.22*blend → 0.12+0.30*blend（缩放响应提速50%+）
- depthYShift：0.32H → 0.40H（远近移动幅度加大）
- 翅膀速度：prox*0.16 → prox*0.20

## 昆仑谣 - s11指尖·入画光斑交互

### 需求
用户提供参考视频（Touchdesigner+Gemini），要求s11尾声场景实现"手电筒"效果：画面默认暗，手势位置处用暖金色径向渐变照亮区域，光斑随手移动、大小随远近变化。

### 实现
- 新增`#spotlight-canvas`(z-index:5, pointer-events:none)，仅s11场景显示
- loop()中每帧渲染：暗色底(rgba(8,6,18,0.78)) + destination-out挖空径向渐变 + source-over叠加暖金辉光
- 光斑半径：baseR=12%屏幕 + prox*(45%-12%)，EMA 0.25跟随_handCenterNX/Y
- 场景提示更新：比1落笔→👁️指尖·入画（手移动照亮山峰，远近控制光环大小）
- **v2修复**：canvas方案(z-index5→14+destination-out)仍不可见，改用纯DOM div方案
  - `#spotlight-mask` div (z-index:14, mix-blend-mode:multiply)
  - 每帧更新CSS radial-gradient的center(x% y%)和circle size(15%~55%)
  - 暗层opacity随prox动态调整(0.70~0.88)，手近暗层淡/光斑大
